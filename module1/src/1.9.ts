{
    //ternery operator || optional chaining || nullish coalescing operator

    const age: number = 19;
    if (age >= 18) {
        console.log('adult');
    }
    else {
        console.log('not adult');

    }

    //ternary operator

    const marriageStatus = "married";
    const isMarried = marriageStatus === "married" ? "yes" : "no";
    console.log(isMarried);

















}